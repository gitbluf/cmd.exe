/**
 * Commands module - public API
 */

// Core commands only - dispatch is handled directly in index.ts
// These are kept for potential future use but not registered as slash commands
export { handleDispatchCommand } from "./dispatch-swarm";
